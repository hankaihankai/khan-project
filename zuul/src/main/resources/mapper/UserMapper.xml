<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.khan.zuul.mapper.UserMapper">
    <resultMap id="baseMap" type="com.khan.zuul.entity.User">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <collection property="authorities" javaType="ArrayList" columnPrefix="role_" ofType="com.khan.zuul.entity.Role">
            <result property="id" column="id"/>
            <result property="name" column="name"/>
        </collection>
    </resultMap>
    <select id="selectByUserName" resultMap="baseMap">
        select u.*, r.id role_id, r.name role_name
        from test_user u
        inner join test_user_role u_r on u.id = u_r.user_id
        inner join test_role r on u_r.role_id = r.id
        where u.username = #{username}
    </select>
</mapper>
